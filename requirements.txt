# Core FastAPI
fastapi==0.115.0
uvicorn==0.30.6
python-dotenv==1.0.1

# HTTP client for n8n calls
httpx==0.27.0

# Database
sqlalchemy==2.0.36
# psycopg2-binary==2.9.9  # PostgreSQL 의존성 - K8s에서 사용시 주석 해제

# Authentication
pyjwt==2.9.0

# Workflow (KB 전용)
langgraph==0.2.45

# Testing
pytest==8.3.3

# Legacy (사용하지 않음)
# websockets==12.0
# beautifulsoup4==4.12.3  # 웹 검색 비활성화
# tenacity==9.0.0
# langchain-openai==0.2.8  # n8n 통합으로 불필요
# scikit-learn, numpy, scipy  # C++ 컴파일러 필요 - 제거됨


